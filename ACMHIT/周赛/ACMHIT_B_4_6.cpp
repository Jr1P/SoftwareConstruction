#include <stdio.h>
#include <math.h>
using namespace std;
const int MAXQ = 5e3+5;
const int MAXL = 1000+5;

int t[MAXL][MAXL];

int main()
{
	char ch;
	int x, y, r, q;
	scanf("%d", &q);
	while (q--)
	{
		scanf("%c%d%d%d", &ch, &x, &y, &r);
		if (ch == 'A')
		{

		}
		else
		{
			
		}
	}

	return 0;
}