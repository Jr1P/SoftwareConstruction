#include <stdio.h>
using namespace std;
const MAXN = 100000 + 1;

int main()
{
	int n, m, k, l, a[MAXN], cnt[MAXN];
	scanf("%d %d", &n, &m);
	for (int i = 0; i <= n; i++)
	{
		scanf("%d", &a[i]);
		cnt[MAXN]++;
	}
	return 0;
}