#include <cstdio>
#define N 100050

struct Node{
    int l, r, w, lz;
}node[N<<2];

int t, n;
int l, r;

int main() {
    scanf("%d", &t);
    while(t--) {
        scanf("%d", &n);
        for(int i = 1; i <= n; i++) {
            int type;
            scanf("%d", &)
        }
    }
    return 0;
}