#include <cstdio>
#include <cstring>
#define mem(a, b) memset(a, b, sizeof(a))
const int N = 105;

struct Edge {
    int v, nxt;
} e[N*N];

int match[N], head[N];
int n, m, k, cnt;

int main() {
    int T;
    scanf("%d", &T);
    while(T--) {
        scanf("%d%d%d", &n, &k, &m);
        
    }
    return 0;
}