#include <cstdio>
const int MAXN = 1e5+1;
int a[MAXN], b[MAXN], pos_a[MAXN], pos_b[MAXN], n, q; // apa[]为公寓, 1~n是男寝, n+1~2n是女寝

int main()
{
	while(scanf("%d", &n) != EOF)
	{
		
		for(int i = 1; i <= n; i++)
			scanf("%d", &a[i]);
		for(int i = 1; i <= n; i++)
			scanf("%d", &b[i]);
		scanf("%d", &q);
		for(int i = 1; i <= q; i++)
		{
			int l1, r1, l2, r2;

		}
	}
	return 0;
}